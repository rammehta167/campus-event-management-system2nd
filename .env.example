# Server configuration
PORT=5000
NODE_ENV=production

# Database configuration
# Set this to your MongoDB Atlas URI or your local MongoDB URI
MONGODB_URI=mongodb://127.0.0.1:27017/urjotsav

# JWT Configuration
# Set a strong, secure secret key in production
JWT_SECRET=replace_with_a_secure_random_string_for_production
